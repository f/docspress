# Docspress WordPress plugins

WordPress plugins maintained as part of Docspress belong in this directory, with one subdirectory per plugin.

There are no bundled Docspress plugins yet. This placeholder establishes the repository layout without inventing a plugin package. Both this directory and the sibling `theme/` directory are repository distributions and are excluded from the `docspress` npm package.
